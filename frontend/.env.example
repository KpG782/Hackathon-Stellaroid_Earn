NEXT_PUBLIC_STELLAR_RPC_URL=https://soroban-testnet.stellar.org
NEXT_PUBLIC_STELLAR_NETWORK=TESTNET
NEXT_PUBLIC_STELLAR_NETWORK_PASSPHRASE=Test SDF Network ; September 2015

# Fill in once your contract is deployed to testnet.
# Current public trust-layer contract (2026-05 security baseline): CA7P5EPYKC2IW4PCMAH6NRBLHH3WP7AN6WWC3QDRWO4HLE47FAGO6TET
NEXT_PUBLIC_SOROBAN_CONTRACT_ID=

# Wallet address that controls admin-only trust actions such as approving or
# suspending issuers in the Phase 1 trust layer UI.
NEXT_PUBLIC_STELLAR_ADMIN_ADDRESS=

# A funded testnet account used only for simulating read-only contract calls.
# Fund any Stellar G... address at https://friendbot.stellar.org/
NEXT_PUBLIC_STELLAR_READ_ADDRESS=

NEXT_PUBLIC_SOROBAN_ASSET_ADDRESS=
NEXT_PUBLIC_SOROBAN_ASSET_CODE=XLM
NEXT_PUBLIC_SOROBAN_ASSET_DECIMALS=7

NEXT_PUBLIC_STELLAR_EXPLORER_URL=https://stellar.expert/explorer/testnet

# Public canonical origin used by SEO metadata and internal URL validation.
# Keep this as your canonical production domain.
NEXT_PUBLIC_CANONICAL_URL=https://stellaroid.tech

# Fee sponsorship is disabled for public browser clients by default.
# Only enable for trusted server-to-server callers with a bearer token.
# FEE_SPONSOR_SECRET=S...
# FEE_SPONSOR_TOKEN=generate-a-long-random-server-token
# NEXT_PUBLIC_FEE_SPONSOR_ADDRESS=G...

# ── Passkey smart wallets for graduates (APAC spec §4, Pillar 2) ──────────────
# Network-agnostic: set the values for THIS deployment's network (testnet or
# mainnet). Graduates fall back to Freighter until a wallet WASM hash is set.
#
# Hash of the deployed smart-wallet contract WASM (per network). Setting a valid
# 64-hex value turns passkeys ON. Deploy the passkey-kit wallet contract to your
# network and paste its WASM hash here.
# NEXT_PUBLIC_PASSKEY_WALLET_WASM_HASH=
# Optional kill switch — set to "false" to force passkeys off even if a hash is set.
# NEXT_PUBLIC_ENABLE_PASSKEYS=
#
# Server-only relayer (OpenZeppelin Relayer Channels plugin) — fee-sponsors the
# wallet deploy so graduates need zero XLM. NEVER NEXT_PUBLIC_*, never committed.
# PASSKEY_RELAYER_URL=https://your-relayer.example
# PASSKEY_RELAYER_API_KEY=
# Set to "channels" for a self-hosted OZ Relayer; omit for the managed service.
# PASSKEY_RELAYER_PLUGIN_ID=

# ── USDC payouts (APAC spec §5, Pillar 3) ────────────────────────────────────
# Payout detection keys on the USDC issuer. Defaults to Circle's canonical
# issuer for the active network (testnet/mainnet); override only for a different
# anchor's USD stablecoin.
# NEXT_PUBLIC_USDC_ISSUER=
